# 1. LED点灯

1. lib \ .h文件最下方一般是所有函数的声明
2. RCC常用的只有AHB AB1 AB2的外部时钟控制的三个函数，三条总线
3. GPIO在APB2总线上

```c
typedef enum
{ GPIO_Mode_AIN = 0x0,//模拟
  GPIO_Mode_IN_FLOATING = 0x04,
  GPIO_Mode_IPD = 0x28,//下拉
  GPIO_Mode_IPU = 0x48,
  GPIO_Mode_Out_OD = 0x14,//开漏
  GPIO_Mode_Out_PP = 0x10,//推挽
  GPIO_Mode_AF_OD = 0x1C,//复用
  GPIO_Mode_AF_PP = 0x18
}GPIOMode_TypeDef;
```

4. 内带led是低电平亮灯

```c
void GPIO_DeInit(GPIO_TypeDef* GPIOx);//GPIO复位
void GPIO_AFIODeInit(void);
void GPIO_Init(GPIO_TypeDef* GPIOx, GPIO_InitTypeDef* GPIO_InitStruct);//GPIO初始化，结构体参数确定模式，引脚，频率
void GPIO_StructInit(GPIO_InitTypeDef* GPIO_InitStruct);//给结构体复位默认值

uint8_t GPIO_ReadInputDataBit(GPIO_TypeDef* GPIOx, uint16_t GPIO_Pin);
uint16_t GPIO_ReadInputData(GPIO_TypeDef* GPIOx);
uint8_t GPIO_ReadOutputDataBit(GPIO_TypeDef* GPIOx, uint16_t GPIO_Pin);
uint16_t GPIO_ReadOutputData(GPIO_TypeDef* GPIOx);

void GPIO_SetBits(GPIO_TypeDef* GPIOx, uint16_t GPIO_Pin);//pin设置高电平
void GPIO_ResetBits(GPIO_TypeDef* GPIOx, uint16_t GPIO_Pin);
void GPIO_WriteBit(GPIO_TypeDef* GPIOx, uint16_t GPIO_Pin, BitAction BitVal);//pin设置高低电平，可以搭配逻辑判断条件，可以是有特定功能的一组，且都是高或者低
void GPIO_Write(GPIO_TypeDef* GPIOx, uint16_t PortVal);//GPIO设置高低电平，且传入的全部pin的值的或结果，而不是pin序号的或结果，是GPIOx全部，可以分别高低，并且同时实现，多用于lcd显示传输

void GPIO_PinLockConfig(GPIO_TypeDef* GPIOx, uint16_t GPIO_Pin);//锁定pin
void GPIO_EventOutputConfig(uint8_t GPIO_PortSource, uint8_t GPIO_PinSource);
void GPIO_EventOutputCmd(FunctionalState NewState);
void GPIO_PinRemapConfig(uint32_t GPIO_Remap, FunctionalState NewState);//重映射
void GPIO_EXTILineConfig(uint8_t GPIO_PortSource, uint8_t GPIO_PinSource);//EXIT中断
void GPIO_ETH_MediaInterfaceConfig(uint32_t GPIO_ETH_MediaInterface);
```

5. 按位或|实现多个值同时操作，进行宏定义
6. 配置的结构体可以复用
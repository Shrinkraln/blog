---
title: 12-2 BKP和RTC
tags: ['stm32']
pubDate: 2026-06-30
---

# 1. BKP备份寄存器

1. 存储用户数据
2. 当VDD切断，仍然由VBAT供电（备用电池电源），没有备用电源可以接VDD及一个100nF的滤波电容
3. 不会被复位刷新
4. TAMPER安全防拆，侵入检测
5. 只有29或84字节
6. 基本结构

![image-20260305124252912](C:\Users\LENOVO\AppData\Roaming\Typora\typora-user-images\image-20260305124252912.png)

# 2， RTC实时时钟

1. 独立的定时器，为系统提供时钟日期
2. 有32位可编程计数器，时间戳的秒
3. 20位的预分频器，RTCCLK分频得到1Hz
   1. HSE（8M）/128；多于系统时钟
   2. LSE32.768kHz在自然溢出后就可以得到1Hz；专供RTC使用，而且VBAT可以供电
   3. LSI 40kHz；多用WWDG
4. 框图
   1. 灰色是后备部分
   2. DIV自减，PRL自动重装，DIV0后，此PRL获取重装（配置分频系数就是修改这里
   3. RTC_Alarm即可以生成中断，又可以闹钟功能唤醒STM，在唤醒后又写入实现循环闹钟

![image-20260305125231845](C:\Users\LENOVO\AppData\Roaming\Typora\typora-user-images\image-20260305125231845.png)

![image-20260305125833730](C:\Users\LENOVO\AppData\Roaming\Typora\typora-user-images\image-20260305125833730.png)

5. 注意事项
   1. 执行以下操作将使能对BKP和RTC的访问： 设置RCC_APB1P的PWREN和BKPEN，使能PWR和BKP时钟；设置PWR_CR的DBP，使能对BKP和RTC的访问
   2. 若在读取RTC寄存器时，RTC的APB1接口曾经处于禁止状态，则软件首先必须等待RTC_CRL寄存器中的RSF位（寄存器同步标志）被硬件置1
   3. 必须设置RTC_CRL寄存器中的**CNF**位，使RTC进入配置模式后，才能写入RTC_PRL、RTC_CNT、RTC_ALR寄存器
   4. 对RTC任何寄存器的写操作，都必须在前一次写操作结束后进行。可以通过查询RTC_CR寄存器中的RTOFF状态位，判断RTC寄存器是否处于更新中。仅当RTOFF状态位是1时，才可以写入RTC寄存器。（PCLK1和RTCCLK时钟不同步